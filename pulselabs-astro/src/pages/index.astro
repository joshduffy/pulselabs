---
import Layout from '../layouts/Layout.astro';
---

<Layout title="PulseLab - Privacy-First Women's Health">
  <!-- Hero Section -->
  <section class="min-h-screen flex flex-col items-center justify-center text-center px-6 md:px-8 lg:px-12 pt-32 pb-20 relative">
    <div class="inline-flex items-center gap-2 px-4 py-2 bg-white border border-[rgba(0,180,160,0.2)] rounded-full text-sm text-[var(--text-medium)] mb-8 shadow-sm">
      <span class="w-2 h-2 bg-[var(--primary)] rounded-full animate-pulse"></span>
      Privacy-first women's health
    </div>

    <h1 class="text-4xl md:text-6xl lg:text-7xl font-extrabold leading-tight mb-6 text-[var(--text-dark)]">
      Your data is<br><span class="gradient-text">yours alone.</span>
    </h1>

    <p class="text-lg md:text-xl text-[var(--text-medium)] max-w-xl mb-10">
      We build apps that never track, never share, and never compromise.
      Pay once, own forever. No subscriptions, no ads, no data harvesting.
    </p>

    <div class="flex gap-4 flex-wrap justify-center">
      <a href="#products" class="btn btn-primary">
        <span>View Our Apps</span>
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14M12 5l7 7-7 7"/>
        </svg>
      </a>
      <a href="#philosophy" class="btn btn-secondary">Our Philosophy</a>
    </div>

    <div class="absolute bottom-8 flex flex-col items-center gap-2 text-[var(--text-light)] text-xs animate-bounce">
      <span>Scroll to explore</span>
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 5v14M5 12l7 7 7-7"/>
      </svg>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section id="philosophy" class="py-20 md:py-24 px-6 md:px-8 lg:px-12 bg-white">
    <div class="container">
      <div class="text-center mb-16">
        <span class="section-label">Our Philosophy</span>
        <h2 class="section-title">Built Different, By Design</h2>
        <p class="section-subtitle">
          In a world where apps exploit your data, we choose a radically simple approach:
          build great software and charge a fair price.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="value-card">
          <div class="value-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            </svg>
          </div>
          <h3>Privacy by Default</h3>
          <p>We can't sell your data because we never collect it. Everything stays on your device. No servers, no sync, no "anonymous" analytics.</p>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="12" y1="1" x2="12" y2="23"/>
              <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
            </svg>
          </div>
          <h3>Honest Pricing</h3>
          <p>One price, yours forever. No subscriptions bleeding you dry, no ads interrupting your day, no premium tiers to unlock basic features.</p>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
            </svg>
          </div>
          <h3>Crafted with Care</h3>
          <p>We'd rather ship one exceptional app than ten mediocre ones. Every detail matters. Every interaction is considered. Quality over quantity, always.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Products Section -->
  <section id="products" class="py-20 md:py-24 px-6 md:px-8 lg:px-12 bg-[var(--bg-light)]">
    <div class="container">
      <div class="text-center mb-16">
        <span class="section-label">Our Products</span>
        <h2 class="section-title">Apps We're Proud Of</h2>
        <p class="section-subtitle">
          Software built with intention, designed to last.
        </p>
      </div>

      <!-- Sync Product -->
      <div class="product-card">
        <div class="product-info">
          <span class="product-badge">Available on iOS</span>
          <h3 class="product-name">Sync</h3>
          <p class="product-tagline">Find your peak.</p>
          <p class="product-description">
            A cycle tracker that predicts your peak days for intimacy — and never shares
            a byte of your data. Beautiful, intuitive, and completely private.
          </p>

          <ul class="product-features">
            <li><span class="check">✓</span> 100% offline — works without internet</li>
            <li><span class="check">✓</span> No account or signup required</li>
            <li><span class="check">✓</span> Zero ads, zero tracking, zero compromises</li>
            <li><span class="check">✓</span> One-time purchase, yours forever</li>
          </ul>

          <div class="product-cta">
            <a href="/sync" class="btn btn-primary">Learn More</a>
            <div class="price-tag">$1.99 <span>one time</span></div>
          </div>
        </div>

        <div class="product-visual">
          <div class="phone-mockup">
            <div class="phone-screen">
              <div class="phone-notch"></div>
              <div class="phone-app-icon bg-gradient-to-br from-[var(--primary)] to-[var(--accent)]">
                <svg viewBox="0 0 64 64" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-14 h-14">
                  <path d="M8 32 L18 32 L22 32 L26 12 L30 52 L34 20 L36 32 L38 44 L42 12 L46 52 L50 32 L54 32 L56 32" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>
                  <circle cx="32" cy="32" r="4" fill="white"/>
                </svg>
              </div>
              <div class="phone-app-name">Sync</div>
              <div class="phone-app-tagline text-[var(--accent)]">Find your peak.</div>
            </div>
          </div>
        </div>
      </div>

      <!-- Ember Product -->
      <div class="product-card ember mt-16">
        <div class="product-visual order-first md:order-none">
          <div class="phone-mockup ember-phone">
            <div class="phone-screen">
              <div class="phone-notch"></div>
              <div class="phone-app-icon overflow-hidden">
                <img src="/ember/icon.png" alt="Ember" class="w-full h-full rounded-3xl">
              </div>
              <div class="phone-app-name">Ember</div>
              <div class="phone-app-tagline text-[var(--ember-accent)]">Your body, illuminated.</div>
            </div>
          </div>
        </div>

        <div class="product-info">
          <span class="product-badge ember-badge">Available on iOS</span>
          <h3 class="product-name">Ember</h3>
          <p class="product-tagline ember-tagline">Your body, illuminated.</p>
          <p class="product-description">
            Track perimenopause and menopause symptoms. Discover patterns in your health data.
            Generate reports for your healthcare provider — all without sharing a single byte.
          </p>

          <ul class="product-features">
            <li><span class="check ember-check">✓</span> Track 20+ symptoms with pattern discovery</li>
            <li><span class="check ember-check">✓</span> Generate PDF reports for your doctor</li>
            <li><span class="check ember-check">✓</span> Sync with Apple Health & Apple Watch</li>
            <li><span class="check ember-check">✓</span> 100% private — all data stays on device</li>
          </ul>

          <div class="product-cta">
            <a href="/ember" class="btn btn-primary ember-btn">Learn More</a>
            <div class="price-tag ember-price">$9.99 <span>one time</span></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials Section -->
  <section class="py-20 px-6 md:px-8 lg:px-12 bg-white">
    <div class="container">
      <div class="text-center mb-12">
        <span class="section-label">What Users Say</span>
        <h2 class="section-title">Loved by People Who Value Privacy</h2>
      </div>

      <div class="testimonial-grid">
        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Finally, an app that doesn't want my email, my data, or my money every month. Sync just works, and I know my data is safe."</p>
          <div class="testimonial-author">
            <span class="author-name">Sarah M.</span>
            <span class="author-app">Sync User</span>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"Ember has been a game-changer for tracking my perimenopause symptoms. The doctor reports helped me have real conversations with my healthcare provider."</p>
          <div class="testimonial-author">
            <span class="author-name">Jennifer L.</span>
            <span class="author-app">Ember User</span>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="testimonial-stars">★★★★★</div>
          <p class="testimonial-text">"After reading about data breaches with other apps, I switched to Sync. Peace of mind knowing my intimate data stays on my device."</p>
          <div class="testimonial-author">
            <span class="author-name">Michelle K.</span>
            <span class="author-app">Sync User</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Newsletter Section -->
  <section class="py-20 px-6 md:px-8 lg:px-12 bg-[var(--bg-light)]">
    <div class="container max-w-2xl text-center">
      <h2 class="text-2xl font-bold text-[var(--text-dark)] mb-4">Stay in the Loop</h2>
      <p class="text-[var(--text-medium)] mb-8">Get occasional updates about new features, health tips, and privacy news. No spam, ever.</p>

      <form action="https://formspree.io/f/newsletter" method="POST" class="newsletter-form">
        <input type="email" name="email" placeholder="your@email.com" required class="newsletter-input" />
        <button type="submit" class="btn btn-primary">Subscribe</button>
      </form>
      <p class="newsletter-note">We respect your privacy. Unsubscribe anytime.</p>
    </div>
  </section>

  <!-- Trust Section -->
  <section class="py-20 px-6 md:px-8 lg:px-12 bg-[var(--text-dark)] text-white">
    <div class="container text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">Why Pay for Apps?</h2>
      <p class="text-lg text-white/70 max-w-xl mx-auto mb-12">
        When an app is free, you're the product. Your data gets sold to advertisers,
        brokers, and who knows who else. We believe there's a better way.
      </p>

      <div class="flex justify-center gap-12 flex-wrap">
        <div class="trust-badge">
          <div class="trust-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
              <circle cx="12" cy="12" r="3"/>
              <line x1="1" y1="1" x2="23" y2="23"/>
            </svg>
          </div>
          <span class="trust-text">We can't see your data</span>
        </div>
        <div class="trust-badge">
          <div class="trust-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="4.93" y1="4.93" x2="19.07" y2="19.07"/>
            </svg>
          </div>
          <span class="trust-text">We can't sell your data</span>
        </div>
        <div class="trust-badge">
          <div class="trust-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
              <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            </svg>
          </div>
          <span class="trust-text">We never have your data</span>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .value-card {
    background: var(--bg-light);
    border-radius: var(--radius-md);
    padding: 2.5rem;
    position: relative;
    overflow: hidden;
    transition: all 0.3s;
  }

  .value-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--gradient);
    transform: scaleX(0);
    transition: transform 0.3s;
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-md);
  }

  .value-card:hover::before {
    transform: scaleX(1);
  }

  .value-icon {
    width: 56px;
    height: 56px;
    background: var(--gradient);
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .value-card h3 {
    font-size: 1.35rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--text-dark);
  }

  .value-card p {
    color: var(--text-medium);
    font-size: 1rem;
    line-height: 1.7;
  }

  /* Product Cards */
  .product-card {
    background: var(--bg-white);
    border-radius: var(--radius-lg);
    padding: 4rem;
    box-shadow: var(--shadow-lg);
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .product-card::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(255,107,157,0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .product-card.ember::before {
    background: radial-gradient(circle, rgba(212, 88, 58, 0.08) 0%, transparent 70%);
  }

  @media (max-width: 900px) {
    .product-card {
      grid-template-columns: 1fr;
      padding: 2.5rem;
      text-align: center;
    }
  }

  .product-badge {
    display: inline-block;
    padding: 0.375rem 0.875rem;
    background: rgba(0, 180, 160, 0.1);
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--primary);
    margin-bottom: 1.25rem;
  }

  .ember-badge {
    background: rgba(212, 88, 58, 0.1);
    color: var(--ember-primary);
  }

  .product-name {
    font-size: 2.5rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
    color: var(--text-dark);
  }

  .product-tagline {
    font-size: 1.25rem;
    color: var(--accent);
    font-style: italic;
    margin-bottom: 1.5rem;
  }

  .ember-tagline {
    color: var(--ember-accent);
  }

  .product-description {
    font-size: 1.1rem;
    color: var(--text-medium);
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .product-features {
    list-style: none;
    margin-bottom: 2rem;
  }

  .product-features li {
    padding: 0.625rem 0;
    color: var(--text-medium);
    display: flex;
    align-items: center;
    gap: 0.875rem;
    font-size: 1rem;
  }

  @media (max-width: 900px) {
    .product-features li {
      justify-content: center;
    }
  }

  .check {
    width: 24px;
    height: 24px;
    background: var(--gradient);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 0.75rem;
    flex-shrink: 0;
  }

  .ember-check {
    background: var(--gradient-ember);
  }

  .product-cta {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
  }

  @media (max-width: 900px) {
    .product-cta {
      justify-content: center;
    }
  }

  .price-tag {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--primary);
  }

  .price-tag span {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-light);
  }

  .ember-price {
    color: var(--ember-primary);
  }

  .ember-btn {
    background: var(--gradient-ember);
    box-shadow: 0 4px 20px rgba(212, 88, 58, 0.35);
  }

  .ember-btn:hover {
    box-shadow: 0 8px 30px rgba(212, 88, 58, 0.45);
  }

  /* Phone Mockup */
  .product-visual {
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    z-index: 1;
  }

  .phone-mockup {
    width: 280px;
    height: 560px;
    background: linear-gradient(180deg, #1A3A4A 0%, #0D1F28 100%);
    border-radius: 40px;
    padding: 12px;
    box-shadow: var(--shadow-lg), inset 0 0 0 3px rgba(255,255,255,0.1);
    position: relative;
  }

  .ember-phone {
    background: linear-gradient(180deg, #6B3A3A 0%, #2A1215 100%);
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: var(--bg-light);
    border-radius: 32px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .phone-notch {
    position: absolute;
    top: 8px;
    width: 100px;
    height: 28px;
    background: #1A3A4A;
    border-radius: 20px;
  }

  .phone-app-icon {
    width: 100px;
    height: 100px;
    border-radius: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
    margin-bottom: 1.5rem;
  }

  .phone-app-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text-dark);
  }

  .phone-app-tagline {
    font-size: 0.9rem;
    font-style: italic;
  }

  /* Trust Badges */
  .trust-badge {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .trust-icon {
    width: 72px;
    height: 72px;
    background: rgba(255,255,255,0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid rgba(255,255,255,0.2);
  }

  .trust-text {
    font-weight: 600;
    font-size: 1rem;
  }

  /* Testimonials */
  .testimonial-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 768px) {
    .testimonial-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .testimonial-card {
    background: var(--bg-light);
    border-radius: var(--radius-md);
    padding: 2rem;
    text-align: left;
  }

  .testimonial-stars {
    color: #F59E0B;
    font-size: 1.25rem;
    margin-bottom: 1rem;
    letter-spacing: 2px;
  }

  .testimonial-text {
    color: var(--text-medium);
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 1.5rem;
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .author-name {
    font-weight: 600;
    color: var(--text-dark);
  }

  .author-app {
    font-size: 0.85rem;
    color: var(--primary);
  }

  /* Newsletter */
  .newsletter-form {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .newsletter-input {
    padding: 1rem 1.5rem;
    border-radius: 9999px;
    border: 2px solid rgba(0, 180, 160, 0.2);
    font-size: 1rem;
    width: 100%;
    max-width: 300px;
    outline: none;
    transition: border-color 0.2s;
  }

  .newsletter-input:focus {
    border-color: var(--primary);
  }

  .newsletter-note {
    font-size: 0.85rem;
    color: var(--text-light);
    margin-top: 1rem;
  }
</style>
